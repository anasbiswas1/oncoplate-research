# Changelog

## 3.0.0 — engineering implementation

- Implements the accepted v3.0 research-to-app direction; does not reactivate the retired exposure tiers.
- Adds 29 Colab notebooks, reusable Python modules, unit/integration tests, runtime recovery, a 60-fit principal grid and data/reference contracts.
- Preserves existing-source files in documentation and maps the planning run IDs to implementation IDs.
- Uses CSV/JSONL/NPZ as portable interchange. Joint models may have supplemental atomic outputs for the new benchmark. Candidate generation is explicit visual/documentary late fusion, not an unimplemented cross-attention claim.
- Adds independent-reference gates, unknown states, no-oracle feature allowlists, focal-item scope checks, paired group statistics and separate external-cohort handling.
- Supplies a local visual research API, not a released mobile application or validated health product.
- Records real testing status separately from future GPU/data experiments.
